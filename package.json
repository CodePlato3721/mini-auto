{
  "name": "mini-auto",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "mini-auto": "./dist/src/interfaces/cli.js"
  },
  "scripts": {
    "dev": "tsx src/interfaces/cli.ts",
    "demo:evidence": "tsx scripts/produce-demo-evidence.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "playwright": "^1.62.1",
    "zod": "^4.5.4"
  }
}
